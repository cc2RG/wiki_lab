<h1>Comments#index</h1>
<p>Find me in app/views/comments/index.html.erb</p>

<ul>
  
<%current_user.comments.each do |c|%>
<li><%= c.content %> 
  <%= current_user.email %>
| <%= link_to('Edit', edit_comment_path(c)) %>
 <%= button_to('Delete', comment_path(c), method: :delete, data: {confirm: "Are you sure?"})%>
<br>

</li>

<%end%>
</ul>

